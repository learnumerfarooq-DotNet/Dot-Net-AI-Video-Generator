// Generic chat agent template — used for Main Brain, Trend, Script, Video, Shorts agents
// Each agent component follows this EXACT same pattern.
// Only the selector, agentKey, icon, color, and title differ.
